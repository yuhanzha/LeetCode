# Solution:

class Solution(object):
    def addDigits(self, num):
        while num > 9:
            res = 0
            while num >=1:
                res += num % 10
                num = num // 10
            num = res
        return num 
                
